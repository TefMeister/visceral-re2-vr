# Research index

**Last `/gr` pass: 2026-09-09 (estate sweep) — FULL** (board `OPEN` block, INDEX, dossier §8c/§8d and both of this lane's speed topics; two public GitHub source reads)**.** Inbox empty. **One new topic, and it corrects a "by construction" claim §8d rests on** — the movement-speed lever is a PAIR (motion-layer rate *and* a movement-driver `getMoveSpeed` return-scale) and both public implementations write both halves. A second, fully-fetchable implementation was found, so the technique no longer depends on the un-fetchable Nexus page; only the RE2 class NAME does.

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep, SECOND pass today) — FULL** (inbox **drained** — one `/sr` hand-off; one delegated source hunt)**.** ⚠️ **The drop's central premise did not survive checking, and the correction matters more than the confirmation would have.** `/sr` asked me to fetch MarsyApp's LTX key names so Anomaly VR's **per-weapon** secondary-hand IK offset table could be copied for the two-hand grip. **No such table is published**, and the "per weapon, in LTX" clause appears **only in summarizer prose, never in a fetched page body** — downgraded to `[hypothesis]`; the published interface is an **in-headset calibration tab** with global console vars. ⭐ In its place, something better: **H3VR's developer was asked for exactly that per-weapon offset and publicly refused it** on authoring cost, shipping a **global `use gun rig mode`** instead — so H3VR sits in the Onward column, and only two design families are actually published (ignore the rear hand for aim; or a *named attach transform* authored in the asset, Blade & Sorcery style). The transferable judgement is that the cost objection **scales with weapon count**, which makes it decisive for a gun sandbox and weak for us. Correction filed to `/sr`, whose library entry carries the withdrawn clause. ⚠️ **Method:** this is the summarizer-echo failure I filed to the library this morning, having propagated one lane further — a claim that only ever appears in summarizer prose is not `[reported]`. Two 🆕 rows above are from today's first pass; three `engine-research/inbox/` drops from it are still unread there and were left alone. **Practice change, adopted immediately:** one ask per drop file, after `/gs` found today that bundled drops get their cheap half done and their expensive half forgotten — today's Alice drops are split accordingly, and this lane's earlier bundled drops are the pattern it names._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep) — FULL** (board OPEN block re-read after two days and ~60 commits of hand/seam/splice work, INDEX, dossier §8f + the Record-system section, the sibling RE Engine project's tools; one delegated multi-source format hunt)**.** Inbox empty. **Two new topics, both aimed at the ⭐ rows the board is about to spend launches on.** ① **Item 22's central either/or is answered from public sources: `.motlist` motions are addressed by NUMBER — a `(bankID, motionID)` pair — not by a motion-name hash**, agreed by four independent sources; the murmur3 hashing in these files is for **bone** names and the one hash-keyed lookup is **file**-level. So outcome (a) is the predicted one and "frozen legs" is now the *surprising* branch. It also closes §8f's `motSize` question sideways: **our n=5 measurement and the public writers disagree** (shipped files carry a real size; two independent tools emit 0 for every entry) and the reading is that the engine ignores the field — **either way our splice, which preserves each blob's own value, is already correct**. Plus one field §8f does not list (`blending`, "0 to enable repeating" — worth a static read on twelve loop motions), one name for the anonymous u32 at +8 (`motNumber` u16 + **`Switch`** u16 at +0x0A), and confirmation that the `natives/x64`→`natives/stm` root move happened **in the same patch as the 524 bump** (we are already on the right side of it). ⚠️ One worry deliberately **retired rather than raised**: the template's 15 unnamed collection dwords are not per-slot metadata — §8f's own byte-identical Claire-vs-Jill comparison already disproved that. ② **Cross-project: the `.rtex.5` descriptor is fully decoded in `re-village-scope-vr`**, byte-for-byte verified on six shipped RE8 files, so the grime row's "decode the format first" branch is already done — there is **no size-dependent field** in a v5 descriptor — and the sibling has the engine **live-accepting an authored, non-shipped target size**. Two pointers filed to `engine-research/inbox/`. My two 2026-09-02 drops and the 2026-09-05 one are **still** unread there and were left alone._

_Previous: **Last `/gr` pass: 2026-09-05 (estate sweep) — FULL** (inbox drained — one modding verdict; the 2026-09-02 topic flipped 👀 and annotated; one new topic correcting this lane's own negative; ~6 searches + a GitHub API read)**.** The drop's status flip and its RE2 `TreeLayer` naming correction are applied. Its one search question — **public use of `MotionSpeedController`** — is answered **no** `[checked 2026-09-05]`, with the honest caveats that Nexus 403s automated fetch and the type is RE2-only game-side, so that is not evidence against it. **The pass's real result is a correction to our own work:** the 2026-09-02 addendum declared "no turnkey layer-dumper tool exists publicly", having checked EMV Engine and the REFramework book — but **`re9_layer0_diag.lua` sits in the very repo that topic was reading**, by the same author. It hands the native probe three upgrades: `get_Weight` (identify the driving layer by measurement, not by string-matching motion names), `getLayerCount` (a real count method — try the no-underscore spelling first), and **`via.motion.Motion` `PlaySpeed`, a speed property above all layers and engine-level** — which may be the "lever above the layer" the drop was reaching for, better than the RE2-only controller type. Pointer filed to `engine-research/inbox/`. Two 2026-09-02 drops were **still** unread there and were left alone._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep, second pass today) — CHECK-IN** (inbox drained — one `/sr` drop; INDEX + one new topic; two online checks)**.** The revision question `/sr` asked is **already answered in-house** (dossier, 2026-08-31: rev `76298bd` = 2026-03-11, before the GUI-callback regression window) and an independent reconstruction from `TOOLCHAIN.md` agrees. What the date-check never covered is September's Lua data-model fixes — a different question, whose blast radius is the `dev-archive/` recon probes, **not** the five shipped scripts. My two 2026-09-02 drops are still unread in `engine-research/inbox/` and were left alone._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep) — CHECK-IN** (board OPEN block + INDEX)**.** Inbox empty; my two 2026-09-02 drops (the `set_Speed` locomotion lever that answers the "MAIN technical risk" row, and the layer-index caveat) are still unread in `engine-research/inbox/`. **Nothing new, and not searched** — nothing further until those are drained._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep) — CHECK-IN** (board OPEN block + INDEX)**.** Inbox empty. **Nothing new — because the board's own "MAIN technical risk" is already answered and waiting.** That row asks whether a writable locomotion movement-speed blend param exists; my 2026-09-02 drop `2026-09-02-gr-motion-layer-set-speed-is-the-writable-locomotion-lever.md` answers it (`via.motion.MotionLayer.set_Speed`, with the layer-index caveat) and is **still unread in `engine-research/inbox/`**. Nothing further to research until that is drained — the remaining rows are a Lua probe to write and a live object-graph read._
_Previous: **Last `/gr` pass: 2026-09-02 (scoped re-run, this project named explicitly) — FULL.** Followed up the same-day speed-lever topic in place rather than a fresh one: read `re9_movement_speed.lua` directly and confirmed the layer index it hard-codes (**0**), conf…_

_Earlier the same day — CHECK-IN:_ inbox drained (three files): two status flips applied, and the
REFramework GUI-callback regression closed out — the sweep filed it, the modding side date-checked our
pinned revision, and we are **not** affected. No new web research that pass.

Every research topic gathered for this project, newest first. Each row links to a self-contained
write-up in `topics/`. Status tags:

- 🆕 **new** — found, not yet acted on by the modding side.
- 👀 **reviewed** — a modding session has read it and factored it into a decision, but nothing shipped from it yet.
- ✅ **incorporated** — directly led to a real change (code, a test, a note) in one of the other five repos; linked below.
- ❌ **dead end** — checked out, didn't pan out; kept for the record so it isn't re-investigated from scratch.

> **Inbox drained, 2026-09-01 — and one closed loop worth recording.**
>
> - **The two 2026-08-29 rows above move to 👀 reviewed.** Both were drained into
>   `ENGINE-DOSSIER.md` as a new **§8b**, deliberately kept apart from the verified §8 material and
>   headed *"from public sources, NOT yet verified live"* — everything in it stays `[reported]`.
>   Reviewed rather than incorporated, because nothing has been tested in VR yet.
> - **The REFramework `on_pre_gui_draw_element` regression is closed, and we were never exposed.**
>   The `/sr` sweep filed it after spotting that returning `false` from that callback silently
>   stopped hiding GUI elements between **2026-08-19 (PR #1503)** and **2026-08-28 (PR #1809)** — the
>   exact mechanism `visceral_crosshair.lua` ships on. The modding side date-checked our pinned
>   revision the same day: **`76298bd` is dated 2026-03-11, five months before the broken window**,
>   so v0.1.0's crosshair hiding is unaffected and no re-test was forced.
>   **The standing consequence:** any future REFramework upgrade must land on a build from
>   **2026-08-28 or later**. A dossier HABIT bullet now records the general form — *when a
>   HUD-suppression script "does nothing", date-check the framework revision before doubting the
>   Lua*, because the framework under a script can break the script's documented contract without a
>   single log line.

| Date | Topic | Status | Summary |
| --- | --- | --- | --- |
| 2026-09-09 | [The movement-speed lever is a PAIR, and both public implementations write both halves](topics/2026-09-09-the-speed-lever-is-a-pair-and-both-public-implementations-use-both-halves.md) | 🆕 new | ⚠️ **Corrects `ENGINE-DOSSIER.md` §8d.** §8d concludes that because RE2 locomotion is root-motion driven, a layer playback-rate clamp scales travel, leg cycle and footsteps together, so req 4 holds *by construction*; this lane's own 2026-09-02 topic called the `app.MovementDriver.getMoveSpeed` hook "Requiem-specific". **Junh2x's shipping mod installs that hook as a first-class part of the feature**, applying the same factor to the layer write and to the driver's returned speed `[reported 2026-09-09, from source]` — which it would have no reason to do if the layer rate alone moved the character. **`Namsku/re-engine-trainer` does the identical pairing independently** (resolves `app.MovementDriver`, return-hooks `getMoveSpeed`, and writes the same factor as the layer speed, with separate walk/run factors and a multi-frame reset). It does **not** name the RE2 (`app.ropeway`) equivalent, so the `[USER]` Nexus-2391 row stands — but narrowed: the method is now doubly public, and only the **name** is still missing, which our own reflection dump can produce. ⇒ the armed NUM7 probe should ask a second question on the same keypress: does a movement-driver-shaped `app.ropeway` type expose a `getMoveSpeed`-shaped float getter? |
| 2026-09-07 | [Two-hand occlusion: the per-weapon offset table is unconfirmed, and one studio refused it](topics/2026-09-07-two-hand-occlusion-the-per-weapon-offset-table-is-unconfirmed-and-one-studio-refused-it.md) | 🆕 new | **Drains `/sr`'s hand-off — and withdraws the half of it that was about to become a design decision.** The problem stands: with a rifle held naturally the support-hand controller sits directly behind the trigger-hand one along the headset's line of sight, degrades, and the weapon jitters — Tefa observed it live in the RE Village scope session `[reported 2026-09-05, n=1 observer]`, and dossier §8e means we inherit it when the two-hand latch comes back on. `/sr` asked this lane to fetch MarsyApp's LTX key names so a **per-weapon offset table** could be copied. ⚠️ **No such table is published, and the premise did not survive checking.** Anomaly VR's anti-occlusion secondary-hand IK is real `[reported 2026-09-07]`, but no LTX section, key, vector shape or example block is published anywhere reachable, and the *"per weapon, in LTX"* clause appeared **only in search-engine summarizer prose, never in a fetched page body** — downgraded to `[hypothesis]`. What *is* published points elsewhere: `F11 → VR Tools` with calibration tabs (**Body Gear, Secondary IK, Detector Holster, Body Holster, Bolt**) and an in-game MCM section, i.e. **a user-calibrated runtime value**, with every published console var global. The emptiness is trustworthy (an open-ended fetch returned specifics no summarizer would invent — asymmetric frustum, shadow maps shared between eyes, eight OpenXR profiles); the mod is closed-source and its `README.txt` ships **inside the archive**, out of reach under rule 1. ⭐ **The replacement finding is stronger than the one sought:** an H3VR player asked Anton Hand / RUST LTD for exactly this per-weapon offset and **the developer declined** — *"nothing I can do about this that wouldn't be incredibly time consuming, and require me to generate an extra entire set of manual poses"* — shipping a **global `use gun rig mode`** instead, which makes every gun's forward direction consistent and stops the foregrip determining the facing angle (breaking lever actions, which it says plainly). **So H3VR is in the Onward camp, not the MarsyApp camp.** Only two design families are actually published: *ignore the rear hand for aim* (Onward, H3VR), and *a named second-hand attach transform authored in the asset* (Blade & Sorcery's `FirearmSecondaryHandle` + `weaponHoldPositionOffset`) — **a numeric per-weapon table is published by nobody**. For us: the authoring-cost objection **scales with weapon count**, so it is decisive for a gun sandbox and weak for Visceral — but start from the global behaviour anyway, and if a per-weapon term is ever wanted, author it as a named attach transform, not as config numbers. Offset *direction* stays an in-headset knob (MarsyApp says *spread apart*, Tefa's reading is *above*; nothing confirms the geometry). ⚠️ Nobody publishes how long an occluded controller coasts on its IMU — Meta names occlusion as worst-case and gives no figure. Correction filed to `/sr`, whose library entry carries the withdrawn clause. |
| 2026-09-07 | [The shipped detail-map parameters are public — and our BC4 mask is a channel gamble](topics/2026-09-07-the-shipped-detail-map-parameters-are-public-and-the-bc4-mask-is-a-channel-gamble.md) | 🆕 new | ⚠️ **Concerns a change already deployed and unseen** (the 2026-09-06 15:06 `pl1000_Jacket_MSK1` mask). ✅ **The `[PD]` texel-density row's mechanism is confirmed: the detail tile is UV-space** `[inferred-static 2026-09-07]` — a single scalar `Detail_UVScale`, the whole `Record_Player.mmtr` overlay family UV-scaled with `*_UseSecondaryUV` companions, and later titles spelling it out (`DetailMap_Tiling_Offset` in RE4R, `DetailMap_Rotation`/`_Scale` in RE9). No triplanar path exists anywhere in RE Engine. **So a ×1.6 texel-density step across the wrist does step the tile's grain there.** ✏️ **Two names we use do not exist in RE2/RE2RT**: there is no `DetailNormalMap` and no `DetailIntensity` — the slot is **`DetailMap`** (packed) and the strengths are **`Detail_Normal_Intensity`** / **`Detail_AO_Intensity`**. ⭐ **The shipped `pl1000_Jacket_Mat` set is published** (NSACloud's RE Mesh Editor presets): `DetailMap` → `NullDetail.tex`, `DetailMaskMap` → `NullWhite.tex`, `Detail_UVScale` **0.25**, `Detail_Normal_Intensity` 0.52, `Detail_AO_Intensity` 0.0. ⚠️ **Two discrepancies to check locally, not to accept**: the board says `Detail_UVScale` 0.5, and the preset's `DetailMap` is *null* — but the preset is `pl1000_Jacket_**Mat**` while our edit targets `pl1000_Body_**Mat**`, plausibly different materials. Read our own dumped `pl1000.mdf2.21`; it is free and settles both. A third party's snapshot is a strong lead and a poor authority. 🚨 **THE RISK: our 2048 BC4 mask may read as zero everywhere.** BC4_UNORM samples as `(R,0,0,1)`, and **which channel `DetailMaskMap` reads is undocumented publicly** — a genuine negative from a corpus that *does* document `NRRC`/`ATOC`/ALBM channel packing. If the shader reads `.g` or `.b`, the tile dies across the **whole material**, and **that failure looks like success** (palms smoother as intended; regression where nobody looks) `[hypothesis]`. Supporting: no channel-selector property exists (RE Engine ships an explicit vec4 where it wants one), and the engine's own masks are 4-channel `_MSK4` while ours is `_MSK1`. **Fix: ship a uniform mid-grey mask first** — grain halves = BC4 populates the read channel; nothing changes = it reads G/B. The family page's "never read back against the neutral value" rule, applied to a texture. ⭐ **A cheaper control than the row plans:** `Detail_Normal_Intensity` / `Detail_AO_Intensity` are plain floats — zeroing them disables the tile with **no texture edit and no channel gamble**, and AO ships at 0.0 so it is realistically **one number**. Separates the diagnostic from the fix, which is exactly the board's stated hesitation. ⚠️ Nothing public says whether `Detail_UVScale` multiplies or divides. ✅ Also confirms `tex .34`/`mdf2.21` = the **RERT** generation, MDF property typing **by count not tag**, and dual MurmurHash3 (UTF-16 + ASCII) name hashes. ⚠️ **Method note:** one fetch **fabricated a quote** listing the five parameter names it was asked about; caught by a neutral re-fetch and discarded — every name above comes from a source read with a prompt that did not name the string. Filed to `/sr` as an engine-agnostic rule. |
| 2026-09-07 | [`.motlist` motions are addressed by number, not by name hash](topics/2026-09-07-motlist-motions-are-addressed-by-number-not-by-name-hash.md) | 🆕 new | ⭐ **Answers the ⭐ `[FLAT]` item-22 splice row's central either/or before the launch that would decide it, and closes a second of §8f's three open questions.** ✅ **Lookup is numeric — a `(bankID, motionID)` pair** `[reported 2026-09-07, 4 independent sources]`: alphaZomega's MMDK (MotionKey → numeric MotionID within a numeric bankID), CAF's `actor_motion_systems.md` (`changeMotion(bankID, motionID, …)`; `findMotionBankByNameHash` hashes the **motlist**, i.e. file-level), CAF's `motlist_format_guide.md` (resolved by **index position**, name is a plain UTF-16 string, **no motion-name hash field**), and alphaZomega's 010 template structurally (its only murmur3 fields are the **bone** and bone-clip hashes). **So outcome (a) is the predicted one and "frozen / T-posed legs → keyed by name hash" is now the *surprising* branch.** ⚠️ Not a proof — no public source names every dword of the 72-byte collection entry, so a per-slot hash in an unnamed field is not formally excluded `[hypothesis]`. ✏️ **`motSize`: our record and the public one disagree, and that resolves it.** §8f measured a real value (blob minus padding, 0 on each file's first entry, n=5); two independent public writers emit **0 for every entry** in this generation (524 normalises to 486; CAF: only populated in RE2 **v65**). Reading: **the engine ignores the field in v524** `[hypothesis]` — and **either way our splice is already right**, since it preserves each blob's own value. The row's "one-line flag to add" is not a pending fix; outcome (c) reduces to "loose file not taken". 🆕 **One field §8f does not list:** the mot **entry header**'s `blending`, annotated publicly as *"set to 0 to enable repeating"* — a static read on the twelve `KFF_GazingWalk_*` loops, and a candidate cause of a walk that plays once and holds, reachable before the blend-node theory. 🔤 **Names the anonymous u32 at +8**: `motNumber` u16 + **`Switch`** u16 at +0x0A; the template independently confirms §8f's 72-byte stride and one-entry-per-slot. ⛔ **Trap we are already clear of:** the RT patch moved the loose-file root `natives/x64` → **`natives/stm`** *in the same patch that bumped motlist to 524*, and a file under the old root is silently ignored — presenting exactly as outcome (c). Row and tool already use `natives/STM`. 🚫 **Deliberately retired, not raised:** the template's 15 unnamed collection dwords are **not** per-slot metadata — §8f's byte-identical Claire-vs-Jill comparison already disproved it. **No public write-up of a v524 motlist splice exists** `[checked 2026-09-07]`, from searches that returned correct on-topic results for adjacent queries — item 22 is novel, not a re-tread. |
| 2026-09-07 | [The `.rtex.5` descriptor is already fully decoded in the sibling RE Engine project](topics/2026-09-07-the-rtex-descriptor-is-already-fully-decoded-in-the-sibling-re-engine-project.md) | 🆕 new | **Cross-project, and it needed no web search at all.** The `[VR]` grime row's failure branch says *"no dirt at all or a crash = the other fields depend on the size, **decode the format first**"*, and the dossier calls the loose-`.rtex` lever "untested" — but **`re-village-scope-vr` decoded the same version-5 64-byte `RTEX` descriptor on 2026-09-06** off five shipped RE8 `movie/rtex` targets plus `mirror_env` `[measured, n=6]`, and **verified it by reproducing two shipped files byte for byte** `[verified-numerically 2026-09-06]` (`dev-archive/tools/rtex_author.py`). Full map: magic · version `5` · type/dimension **`4`** · DXGI format (`29` SRGB movie targets, `26` R11G11B10_FLOAT for `mirror_env`) · width · height · depth `1` · **mip count `1`** · zeros · two `1.0` floats. ⭐ **So the "decode the format first" clause is already satisfied, and it narrows the row: there is NO size-dependent field** — mip count is a flat `1` across six files spanning 1024² to 1920×1088, no stride, no byte-size, no offset table — so a crash should not be read first as "a field depended on the size"; the config-decides-it and asset-cached branches lead `[inferred-static 2026-09-07]`. ⭐ **And the engine is proven to accept an authored, non-shipped size**: the sibling authored 2560×1448 and 3840×2168 targets existing in no archive, deployed them loose, and the engine allocated and latched 2560 — Tefa in the headset called the picture clearly better `[verified-live 2026-09-06, n=1]`. Same engine, same file version, same loose-file mechanism; the *Record system* specifically is still unverified, so the row stays a real test, but "untested lever" is now too strong. Also: **the name is not a contract** (RE8's `movie_1920_1080` is really 1920×1088), and `rtex_resize.py`'s sixteen-dword dump now identifies the Record target's **format**. **Free next step:** dump the *shipped* `pl1000_body.rtex.5` and compare against the map — agreement upgrades the claim to `[measured]` and lets the row drop the clause outright; disagreement is a better finding still. |
| 2026-09-04 | [The revision question is settled; September's Lua fixes miss the shipped mod](topics/2026-09-04-the-revision-question-is-settled-and-the-september-lua-fixes-miss-the-shipped-mod.md) | 🆕 new | Drains `/sr`'s revision drop. **Half of it was already answered:** the dossier's 2026-08-31 date-check has our pinned rev `76298bd` at **2026-03-11**, months before the 2026-08-19→2026-08-28 window when `on_pre_gui_draw_element` ignored `false` — which is the check that mattered, since the crosshair script has no other mechanism. The build is neither release nor master but a `pd-upscaler` fork build; that fork **still publishes no releases** and was last pushed 2026-08-06 `[verified-live 2026-09-04, n=1 API read]`, so "do not upgrade" stands and the one reason to upgrade does not apply to us. **The new part:** September's array / string-number fixes are a different question the date-check never covered — and **none of the five shipped scripts reads a managed array** `[inferred-static 2026-09-04]`, so the exposure is in the `dev-archive/` recon probes, where a data-model bug returns a wrong answer rather than an error. Adopts `/sr`'s standing rule: record the revision beside every Lua finding. ⚠️ `re-village-scope-vr` has **no revision recorded at all**. |
| 2026-09-05 | [The public layer-dumper we said did not exist is in the repo we already read](topics/2026-09-05-the-public-layer-dumper-we-said-did-not-exist-is-in-the-repo-we-already-read.md) | 🆕 new | ❌ **Corrects this lane's own 2026-09-02 negative.** `Junh2x/RE9-Movement-Speed-Mod` ships **three** autorun scripts, not one: alongside the mod there is **`re9_layer0_diag.lua`** (9,260 B) — a motion-layer property dumper with an in-game UI — plus a character-ID dumper. The 2026-09-02 check searched EMV Engine and the REFramework book for such a tool and concluded none existed, without listing the contents of the repo it was already reading. Three gains for the native probe: **`get_Weight`** identifies the driving layer by measurement rather than by name-matching; **`getLayerCount`** (no underscore, tried first) is a real count method, so enumeration need not probe until `nil`; and 🎯 **`via.motion.Motion` `PlaySpeed`** is a speed property **above all layers** and **engine-level** — a better answer to the drop's "a lever above the layer" than the RE2-only `SurvivorMotionSpeedController`, though only its **getter** is witnessed, so `set_PlaySpeed` is `[hypothesis]`. **The drop's question answered:** no public use of `MotionSpeedController` found `[checked 2026-09-05]` — but Nexus 403s automated fetch and it is an RE2-only game-side type, so that is **not** evidence against it. |
| 2026-09-02 | [A writable speed lever exists: the motion layer's playback speed](topics/2026-09-02-a-writable-speed-lever-exists-the-motion-layers-playback-speed.md) | 👀 reviewed — **modding verdict 2026-09-04:** folded into `ENGINE-DOSSIER.md` as **§8d**, and the board's "MAIN technical risk" row **retired** on the strength of it; the layer-index caveat is now a **probe** question, not a search question. ⚠️ **RE2 naming:** there is **no `via.motion.MotionLayer`** in RE2 — `getLayer` returns **`via.motion.TreeLayer`** `[inferred-static 2026-09-04]`. **Its "no public layer-dumper exists" bullet is corrected by the 2026-09-05 row above.** | ⭐ Junh2x's Requiem "Better Movement Speed" (ported to RE2 on Nexus) writes `set_Speed(k)` on the player's `via.motion.Motion` layer each `LateUpdateBehavior`, gated on `"walk"`/`"run"` in `get_HighestWeightMotionNode():get_MotionName()`; praydog's `re2_smooth_movement.lua` instead writes the body transform per `UpdateMotion`. Root motion means a playback-rate clamp scales travel, leg cycle and footsteps as one — req 4 by construction, not a blend. **Addendum, same day:** the mod hard-codes layer index **0** (unverified for RE2 itself, no public authority to check it against); enemies use the identical API for their own locomotion, not for awareness; no public source ties enemy awareness to player speed at all — a probe recipe is written up. |
| 2026-09-01 | [The FirstPerson settle bug is a lerp that never reaches zero](topics/2026-09-01-the-firstperson-settle-bug-is-a-lerp-that-never-reaches-zero.md) | 🆕 new | Every Visceral release ships `FirstPerson_Enabled=true`, so this is our users' bug. Upstream sets `wanted_camera_shake = 0.0f` in VR, then only **lerps** `m_interp_bone_scale` toward it at `clamp(delta_time*0.05f, …)`; the snap branch tests `bone_scale == 0.0f` exactly, so it never fires. **The CameraShake slider is not a workaround** — VR never reads it. The consistent fix is to add `vr->is_hmd_active()` to that condition, exactly as the `m_interpolated_bone` branch a few lines up already does. Clean candidate for an upstream PR. One unknown left: `m_interp_bone_scale`'s initial value. Line numbers are approximate — the blob view truncated at 1000 and returned a false negative first. |
| 2026-08-29 | [Weapon-equipped state surface](topics/2026-08-29-weapon-equipped-state-surface.md) | 👀 reviewed | The public RE2 class surface for "a weapon is out": `Equipment.<EquipWeapon>k__BackingField`, `implement.Gun`/`Melee`, `IsHold`/`IsReload`, and REFramework's `FireBulletType Camera→AlongMuzzle` flip as the model behavior-change-while-armed. |
| 2026-08-29 | [CAF custom animation framework](topics/2026-08-29-caf-custom-animation-framework.md) | 👀 reviewed | Working public custom-animation system for RE2 with an RE3-style dodge (= roadmap 15 prior art): DynamicMotionBank loading, MotionFsm2 pause, set_Position+warp root motion — and the armed-state gating it lacks. Plus the layer-speed + getMoveSpeed-hook technique for aim-walk speed. |
| 2026-08-29 | [EMV Engine toolkit map](topics/2026-08-29-emv-engine-toolkit-map.md) | 🆕 new | EMV has zero weapon code — its value is instrumentation: Console, Action Monitor, Hooked Method Inspector, Poser, and the RE2 motion-bank path/ID reference, mapped to the armed-state study each roadmap front needs. |

## How to add a topic

1. New file in `topics/`, named `YYYY-MM-DD-short-slug.md`.
2. One row added to the table above, newest at the top.
3. Update the status tag here as it moves through review → incorporated/dead-end (the modding side should update this when it acts on a lead, so the index reflects reality without the research side needing to poll).
